服务商通过[入会组件预授权](https://pay.weixin.qq.com/doc/v3/partner/4015336986.md)接口获取授权的必要参数 token 后，可使用微信支付侧提供的 JS 调起品牌会员入会组件，引导用户完成入会流程（公众号端）。

## 接口说明

支持商户：【普通服务商】

接口名称:  openBusinessView

兼容性表现说明

- 此接口引用 JSAPI 版本1.5.0，引用地址：[https://res.wx.qq.com/open/js/jweixin-1.5.0.js](https://res.wx.qq.com/open/js/jweixin-1.5.0.js)

- 要求用户微信版本>=7.0.5


## 字段说明

### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| --- |  --- | --- | --- |
| businessType | string(32) | true | 【跳转类型】固定值，请传入 wxpayCardMember |
| queryString | string(2048) | true | 【业务参数】需要传递给入会组件的数据。使用 URL 的 query string 方式传递参数，格式为key=value&key2=value2，其中 value、value2 需要进行 UrlEncode 处理。 |
| queryString.token | string(32) | true | 【预授权 token】调用品牌预授权接口返回的预授权 token (1 小时内有效)。 |
| queryString.scene | string(32) | true | 【组件场景值】组件的应用场景值，固定为 `CARD_MEMBER` |

请求示例

```
let wechatInfo = navigator.userAgent.match(/MicroMessenger\/([\d\.]+)/i);
let wechatVersion = wechatInfo[1];

if (compareVersion(wechatVersion, '7.0.5') >= 0) {
	goToWXCardMember();
} else {
	// 提示用户升级微信客户端版本
	window.location.href = 'https://support.weixin.qq.com/cgi-bin/readtemplate?t=page/common_page__upgrade&text=text005&btn_text=btn_text_0';
}

/**
 * 跳转品牌入会组件
 */
function goToWXCardMember() {
    wx.checkJsApi({
        jsApiList: ['openBusinessView'], // 需要检测的 JS 接口列表
        success: function (res) {
        // 以键值对的形式返回，可用的 api 值 true，不可用为 false
        // 如：{"checkResult":{"openBusinessView":true},"errMsg":"checkJsApi:ok"}
        if (res.checkResult.openBusinessView) {
            wx.invoke(
                'openBusinessView', {
                    businessType: 'wxpayCardMember',
                    queryString: 'token=EhhEF0Jt0uDLPK6Cr70KCrJmkTtfPPE2&scene=CARD_MEMBER',
                },
                function (res) {
                	// 从入会组件返回时会执行这个回调函数
                    if (parseInt(res.extraData.errcode) === 0) {
                    	// 返回成功
                        if (res.extraData.data.hasJoinCardMember) {
                            // 用户入会成功
                        } else {
                            // 用户入会失败
                        }
                    } else {
                    	// 返回失败
                    }
                });
            }
        }
    });
 }

 /**
  * 版本号比较
  * @param {string} v1
  * @param {string} v2
  */
function compareVersion(v1, v2) {
    v1 = v1.split('.')
    v2 = v2.split('.')
    const len = Math.max(v1.length, v2.length)

    while (v1.length < len) {
		v1.push('0')
    }
    while (v2.length < len) {
		v2.push('0')
    }

    for (let i = 0; i < len; i++) {
        const num1 = parseInt(v1[i])
        const num2 = parseInt(v2[i])

      	if (num1 > num2) {
       		return 1
      	} else if (num1 < num2) {
        	return -1
      	}
    }

    return 0
 }
```

### 返回参数

触发场景: 用户从品牌会员入会组件页返回到品牌页面。

注意：

- 只有用户点击入会组件页面内返回按钮时，才会带上返回参数；如果用户左滑返回或者点击页面左上角的返回图标返回，则不会带上返回参数。

- 推荐品牌侧以[查询用户会员卡信息](https://pay.weixin.qq.com/doc/v3/partner/4015336980.md)和[会员卡事件通知](https://pay.weixin.qq.com/doc/v3/partner/4015283692.md)返回的结果为准。


extraData  必填 Object

回传商家小程序的业务数据

![](data:image/png;base64,)属性

errcode 必填  number/string(32)

【错误码】由于 iOS 和 Android 实现的差异，errcode 类型可能为 number 或 string

* * *

msg 必填  string(256)

【状态描述】状态描述

* * *

data 选填  Object

【详细信息】详细信息

![](data:image/png;base64,)属性

hasJoinCardMember 必填  boolean

【用户是否入会成功】表示用户入会是否成功