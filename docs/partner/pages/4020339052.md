>更新时间：2026.06.30

## 1、接入准备

### 1.1 接入模式

停车便捷缴费仅支持服务商模式接入，服务商需完成如下前置准备：

| 准备项 | 说明 | 获取路径 |
| --- | --- | --- |
| 服务商 mchid | 服务商在微信支付申请入驻的账号 | [服务商平台](https://pay.weixin.qq.com/partner/public/home) |
| AppID | 服务商小程序 AppID（承接缴费收款页） | [微信公众平台](https://mp.weixin.qq.com/) |
| API v3 密钥 | 用于敏感字段加解密 | 服务商平台自行设置 |
| 商户 API 证书 | 用于请求签名与验签 | 服务商平台下载 |
| 微信支付平台证书 / 公钥 | 用于回包与回调验签 | API 下载 |

### 1.2 签名与验签

沿用微信支付 APIv3 通用签名方案，详见：

- [APIV3接口签名和验签说明文档](https://pay.weixin.qq.com/doc/v3/partner/4012365870.md)

- [如何使用签名/验签工具](https://pay.weixin.qq.com/doc/v3/partner/4012365880.md)


同步车辆入场通知、同步车辆离场通知、同步支付结果通知、同步非临停扩展支付接口统一使用 HTTP POST + JSON \+ `WECHATPAY2-SHA256-RSA2048` Authorization 头格式。

### 1.3 入驻流程

![](https://gtimg.wechatpay.cn/resource/xres/mmpaydoc/static/img/8e8ab7ab055c92c5966e41d6f4298149.png)

1、服务商入驻：与微信支付运营人员完成服务商主体入驻对接

2、停车场进件：

- 方式一：通过进件 API 自助提交

- 方式二：与微信支付运营人员对接


3、运营受理：运营侧审核通过后，车场进入观察期

4、车场数据观察：对车场运营数据进行跟踪评估

5、结果判定：

- 达标：车场自动放量，正常接入使用，达到启用状态

- 不达标：车场自动临时下线，需优化后重新评估


## 2、全流程序列图

### 2.1 入驻阶段

```mermaid
%%{init: { "sequence": { "wrap": true, "wrapPadding": 10, "noteAlign": "left" } } }%%
sequenceDiagram
    autonumber
    actor SP as 停车服务商
    actor WxPay as 微信支付运营
    participant WxPay2 as 微信支付(停车便捷缴费)

    Note over SP,WxPay: 阶段一：服务商入驻（复用微信支付已有商户号与证书体系）

    SP->>WxPay: 提交服务商入驻申请
    WxPay->>WxPay2: 受理服务商入驻

    Note over SP,WxPay2: 阶段二：停车场进件

    alt 半自动进件(存量)
        SP->>WxPay: 提交车场进件资料
        WxPay->>WxPay2: 受理停车场进件
    else API化进件
        SP->>WxPay2: 提交停车场进件申请（车场基础信息+计费规则+优惠+节假日模式）
    end

    WxPay2-->>SP: 下发 parking_id

    Note over WxPay2,WxPay2: 阶段三：车场观察期
    WxPay2->>WxPay2: 进入车场观察期

    alt 观察期内数据达标
        WxPay2-->>SP: 车场自动放量上线
    else 观察期内数据未达标
        WxPay2-->>SP: 车场暂不上线（数据修复后自动重新评估）
    end
```

### 2.2 车辆进场（通知/邀约）

```mermaid
%%{init: { "sequence": { "wrap": true, "wrapPadding": 10, "noteAlign": "left" } } }%%
sequenceDiagram
    autonumber
    actor P1 as 车主
    actor P2 as 闸机
    participant SP as 停车服务商系统
    participant WxPay as 微信支付(停车便捷缴费)

    P1->>P2: 驶入停车场
    P2->>SP: 上报入闸信息
    SP->>WxPay: SyncParkingEntry 同步车辆入场

    alt 用户已开通
        WxPay-)P1: 推送「入场通知」模板消息
    else 未开通 且 在邀约名单 且 未邀约过 且 非月卡车场
        WxPay-)P1: 推送「开通邀约」模板消息
    else 月卡车场 或 已邀约过
        WxPay->>WxPay: 不推送任何消息
    end
```

### 2.3 缴费阶段

```mermaid
%%{init: { "sequence": { "wrap": true, "wrapPadding": 10, "noteAlign": "left" } } }%%
sequenceDiagram
    autonumber
    actor P1 as 车主
    participant MiniApp as 车主服务小程序
    participant MiniApp2 as 服务商小程序
    participant SP as 服务商系统
    participant WxPay as 微信支付
    actor P6 as 闸机

    P1->>MiniApp: 点击入场通知/跳价提醒
    MiniApp->>SP: 获取缴费信息（查费）
    SP-->>MiniApp: 返回应付/已付/停车单号
    P1->>MiniApp2: 点击「去缴费」跳转
    MiniApp2->>SP: 下单
    SP->>WxPay: 下单（商品描述含停车场景）
    WxPay-->>P1: 调起支付
    P1->>WxPay: 完成支付
    WxPay-)SP: 异步通知支付结果
    SP->>WxPay: SyncParkingPaymentResult 同步用户支付
    SP->>P6: 通知闸机自动抬杠
```

### 2.4 车辆离场

```mermaid
%%{init: { "sequence": { "wrap": true, "wrapPadding": 10, "noteAlign": "left" } } }%%
sequenceDiagram
    autonumber
    actor P1 as 闸机
    participant SP as 服务商系统
    participant WxPay as 微信支付

    P1->>SP: 记录出场信息

    alt 先付费后离场（支付与离场分离）
        SP-)WxPay: SyncParkingExit 纯离场（不带支付信息）
    else 离场即支付（支付与离场合并）
        SP-)WxPay: SyncParkingExit 携带支付信息字段
    end
```

### 2.5 非临停支付（独立通路）

```mermaid
%%{init: { "sequence": { "wrap": true, "wrapPadding": 10, "noteAlign": "left" } } }%%
sequenceDiagram
    autonumber
    actor P1 as 车主
    participant SP as 停车服务商系统
    participant WxPay as 微信支付(停车便捷缴费)

    P1->>SP: 购买月卡 / 季卡 / 会员
    SP->>SP: 生成非临停支付单
    Note over SP,WxPay: 独立 HTTP 接口，严禁复用 SyncParkingPaymentResult

    SP->>WxPay: POST /v3/vehicle/parking/events/ext-payment（SyncParkingExtPayment）


    alt 成功
        WxPay-->>SP: 200 SUCCESS
    else 失败
        WxPay-->>SP: 4xx 错误码
    end
	Note over P1,WxPay: 独立通路：无论成败均不触发 C 端进场/邀约/跳价消息；不累加停车单金额
```

## 3、注意事项

接入方必须严格遵守以下注意事项，违反可能会导致数据不达标，车场下线返佣不结算情况：

| 序号 | 开发注意事项 | 影响 |
| --- | --- | --- |
| 1 | 车辆入场、车辆出场、同步支付结果接口为微信支付感知车辆状态的唯一来源，三者缺一不可，若缺少可能导致状态错乱 | 车辆状态错乱，邀约缴费关单异常 |
| 2 | 同一支付信息仅可在「[同步车辆离场](https://pay.weixin.qq.com/doc/v3/partner/4019557725.md)」接口、「[同步支付结果](https://pay.weixin.qq.com/doc/v3/partner/4019557765.md)」接口中二选一上报，不得同时上报 | 支付消息重复上报，导致订单金额错乱、对账异常 |
| 3 | 非临停场景（月卡/季卡/会员等固定缴费模式）使用独立的[非临停支付](https://pay.weixin.qq.com/doc/v3/partner/4020338487.md)接口，不能写入临停支付接口 | 污染临停语义 / 误触发邀约 |
| 4 | 车场闸机抬杆放行逻辑由接入方服务商自行控制，微信侧不直接控闸机 | 放行逻辑不可混淆 |