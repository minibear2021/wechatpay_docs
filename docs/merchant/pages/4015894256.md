# 1\. 目标

通过本教程的学习你应该可以：

- 了解代金券创建、管理、发放、查询的全流程

- 对于代金券批次，你可以：

  - 创建、激活、暂停、重启代金券批次；

  - 设定查询条件，查询的代金券批次列表；

  - 查询某一个指定批次的详细信息、可用商户和可用单品；

  - 下载指定批次代金券的退款明细和核销明细。
- 对于代金券，你可以：

  - 根据商户号查询指定用户拥有的代金券，并可指定批次号、代金券状态等参数查询；

  - 发放指定批次的代金券给用户；

  - 根据代金券号查询代金券详细。
- 除此之外，你还可以：

  - 设置和查询代金券消息的通知地址，接受代金券核销事件的回调通知；

  - 在系统中上传图片并获得图片的 url 地址，图片url可在微信支付营销相关的API使用。

本文档中的代码 Demo 中均使用了平台提供的工具类 WXPayUtility ：[Java\_SDK&开发工具](https://pay.weixin.qq.com/doc/v3/merchant/4014931831.md)

在使用代金券功能之前，请先 [开通代金券功能](https://pay.weixin.qq.com/doc/v3/merchant/4012084133.md)。

# 2\. 前置流程



## 2.1. 设置和查询代金券消息通知地址

如果需要获得代金券核销的实时反馈，商户可以在制券前先设置好代金券消息的通知地址，以接受代金券核销事件的回调通知。

下面给出了设置代金券消息通知地址和查询代金券消息通知地址的示例代码：

```
package com.java.coupon;

import com.java.utils.WXPayUtility; // 引用微信支付工具库，参考：https://pay.weixin.qq.com/doc/v3/merchant/4014931831

// 使用商户平台设置消息代金券消息通知地址接口，参考https://pay.weixin.qq.com/doc/v3/merchant/4012464198
import com.java.demo.SetCallback;
import static com.java.demo.SetCallback.*;

// 使用商户平台查询消息通知地址接口，参考 https://pay.weixin.qq.com/doc/v3/merchant/4012464070
import com.java.demo.QueryCallback;
import static com.java.demo.QueryCallback.*;

public class InitCouponDemo {

    // TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考
    // https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    private static String mchid = "19xxxxxxxx";
    // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013053053
    private static String certificateSerialNo = "1DDE55AD98Exxxxxxxxxx";
    // 商户API证书私钥文件路径，本地文件路径
    private static String privateKeyFilePath = "/path/to/apiclient_key.pem";
    // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013038816
    private static String wechatPayPublicKeyId = "PUB_KEY_ID_xxxxxxxxxxxxx";
    // 微信支付公钥文件路径，本地文件路径
    private static String wechatPayPublicKeyFilePath = "/path/to/wxp_pub.pem";

    public static void main(String[] args) {
        InitCouponDemo demo = new InitCouponDemo();

        // 1. 设置代金券消息通知地址
        String notifyUrl = "https://pay.weixin.qq.com";  // 商户的消息通知地址
        demo.setCallback(mchid, notifyUrl);

      	// 由于系统处理需要时间，建议间隔 1 分钟以上进行操作

        // 2. 可以查询商户设置的消息通知地址
        String notifyUrlResp = demo.queryCallback(mchid);
    }

    // 查询商户号为 queryMchid 的商户的消息通知地址
    // 返回该商户号的消息通知地址
    public String queryCallback(String queryMchid) {

        QueryCallback client = new QueryCallback(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        QueryCallbackRequest request = new QueryCallbackRequest();
        request.mchid = queryMchid;  // 需要查找回调地址的商户号

        try {
            Callback response = client.run(request);

            // 请求成功，返回商户号和消息通知地址
            String mchidResp = request.mchid;
            String notifyUrlResp = response.notifyUrl;

            return notifyUrlResp;

        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试（建议等待一分钟后原单重试）
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();

            return null;
        }
    }

    // 设置商户号 callbackMchid 的消息通知地址为 notifyUrl
    public void setCallback(String callbackMchid, String notifyUrl) {

        SetCallback client = new SetCallback(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        SetCallbackRequest request = new SetCallbackRequest();

        // 请参阅实际文档进行传参 https://pay.weixin.qq.com/doc/v3/merchant/4012464198
        request.mchid = callbackMchid;              // 需要设置消息通知地址的商户号
        request.notifyUrl = notifyUrl;              // 新设置的消息通知地址

        // 如果商户接收营销事件通知的开关。
        // true：开启推送 / false：停止推送
        // 目前暂不支持设置参数为 false，如果想关闭通知请在商户平台关闭券的回调通知
        request._switch = true;

        try {
            SetCallbackResponse response = client.run(request);

            // 请求成功，返回新设置的消息通知地址和更新的时间
            String notifyUrlResp = response.notifyUrl;
            String updateTime = response.updateTime;

        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试（建议等待一分钟后原单重试）
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
        }
    }
}
```

## 2.2. 图片上传（营销专用）

商户可以通过接口上传图片，上传后可以获得图片 url 地址，图片url可在微信支付营销相关的API使用。

上传图片使用到 [wechatpay-apache-httpclient](https://github.com/wechatpay-apiv3/wechatpay-apache-httpclient/tree/master) 项目

下载项目后使用以下代码上传图片到营销平台：

```
package com.wechat.pay.contrib.apache.httpclient;

import static org.apache.http.HttpStatus.SC_OK;
import static org.junit.Assert.assertEquals;

import com.wechat.pay.contrib.apache.httpclient.auth.AutoUpdateCertificatesVerifier;
import com.wechat.pay.contrib.apache.httpclient.auth.PrivateKeySigner;
import com.wechat.pay.contrib.apache.httpclient.auth.WechatPay2Credentials;
import com.wechat.pay.contrib.apache.httpclient.auth.WechatPay2Validator;
import com.wechat.pay.contrib.apache.httpclient.util.PemUtil;
import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;
import java.io.InputStream;
import java.net.URI;
import java.net.URISyntaxException;
import java.nio.charset.StandardCharsets;
import java.security.PrivateKey;
import org.apache.commons.codec.digest.DigestUtils;
import org.apache.http.HttpEntity;
import org.apache.http.client.methods.CloseableHttpResponse;
import org.apache.http.impl.client.CloseableHttpClient;
import org.apache.http.util.EntityUtils;

public class UploadPhoto {

    // 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    private static final String privateKey = "-----BEGIN PRIVATE KEY-----\n"					   // 你的商户私钥
            + "your private key"
            + "-----END PRIVATE KEY-----\n";
    private static final String merchantId = "merchant_id"; 								       // 商户号
    private static final String merchantSerialNumber = "merchant_serial_number";                   // 商户证书序列号
    private static final String apiV3Key = "api_v3_key"; 		                    			   // API V3密钥

    public static void main(String[] args) throws IOException, URISyntaxException {

        PrivateKey merchantPrivateKey = PemUtil.loadPrivateKey(privateKey);

        //使用自动更新的签名验证器，不需要传入证书
        AutoUpdateCertificatesVerifier verifier = new AutoUpdateCertificatesVerifier(
                new WechatPay2Credentials(merchantId, new PrivateKeySigner(merchantSerialNumber, merchantPrivateKey)),
                apiV3Key.getBytes(StandardCharsets.UTF_8));

        CloseableHttpClient httpClient = WechatPayHttpClientBuilder.create()
                .withMerchant(merchantId, merchantSerialNumber, merchantPrivateKey)
                .withValidator(new WechatPay2Validator(verifier))
                .build();

      	// 需要上传的图片路径
        String filePath = "/home/filepath/to/tres.png";

        URI uri = new URI("https://api.mch.weixin.qq.com/v3/marketing/favor/media/image-upload");

        File file = new File(filePath);
        try (FileInputStream fileIs = new FileInputStream(file)) {
            String sha256 = DigestUtils.sha256Hex(fileIs);
            try (InputStream is = new FileInputStream(file)) {
                WechatPayUploadHttpPost request = new WechatPayUploadHttpPost.Builder(uri)
                        .withImage(file.getName(), sha256, is)
                        .build();

                try (CloseableHttpResponse response = httpClient.execute(request)) {
                    assertEquals(SC_OK, response.getStatusLine().getStatusCode());
                    HttpEntity entity = response.getEntity();
                    // 返回上传成功的图片的路径
                    String s = EntityUtils.toString(entity);
                    System.out.println(s);
                }
            }
        }
        httpClient.close();
    }
}
```

# 3\. 代金券批次的管理和发放

## 3.1. 业务流程

商户可以使用相关 API 管理本商户的代金券批次，进行包括创建、激活、暂停、重启以及查询代金券批次相关信息等一系列操作，代金券批次的状态机如下：

![](https://gtimg.wechatpay.cn/resource/xres/mmpaydoc/static/img/c4a9ae83676e059afac37e9a1ed5331a.png)

需要注意的是：

- 对于激活代金券批次：

  - 如果是预充值代金券，激活时会从商户运营账户余额中锁定本批次的营销资金。

  - 如果是免充值代金券，激活时不锁定资金。
- 对于发放代金券操作：

  - 当在代金券处于“运行中”状态时，商家可以进行发放代金券操作。

  - 商户可在H5活动页面、商户小程序、商户APP等自有场景内调用该接口完成发券，商户默认只允许发放本商户号（调用发券接口的商户号）创建的代金券，如需发放其他商户商户创建的代金券，请参考 [常见问题](https://developers.weixin.qq.com/community/pay/doc/0000046f16862087608adcdf959c08) Q1。

  - 跨商户发券时，请求参数中除了 stock\_id 和 stock\_creator\_mchid 为创建方提供的数据，其他的所有调用数据都由发放方提供。

## 3.2. 示例代码

```
package com.java.coupon;

import com.java.utils.WXPayUtility; // 引用微信支付工具库，参考：https://pay.weixin.qq.com/doc/v3/merchant/4014931831
// 使用商户平台创建代金券批次接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012534633
import com.java.demo.CreateCouponStock;
import static com.java.demo.CreateCouponStock.*;

// 使用商户平台激活代金券接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012460137
import com.java.demo.StartStock;
import static com.java.demo.StartStock.*;

// 使用商户平台暂停批次接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012460305
import com.java.demo.PauseStock;
import static com.java.demo.PauseStock.*;

// 使用商户平台重启代金券批次接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012460411
import com.java.demo.RestartStock;
import static com.java.demo.RestartStock.*;

// 使用商户平台发放指定批次的代金券接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012463767
import com.java.demo.SendCoupon;
import static com.java.demo.SendCoupon.*;

import java.util.ArrayList;

public class CouponManagementDemo {

    // TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考
    // https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    private static String mchid = "19xxxxxxxx";
    // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013053053
    private static String certificateSerialNo = "1DDE55AD98Exxxxxxxxxx";
    // 商户API证书私钥文件路径，本地文件路径
    private static String privateKeyFilePath = "/path/to/apiclient_key.pem";
    // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013038816
    private static String wechatPayPublicKeyId = "PUB_KEY_ID_xxxxxxxxxxxxx";
    // 微信支付公钥文件路径，本地文件路径
    private static String wechatPayPublicKeyFilePath = "/path/to/wxp_pub.pem";

    public static void main(String[] args) {

        CouponManagementDemo demo = new CouponManagementDemo();

        // 1. 创建一个新的代金券批次
        // 创建后返回创建成功的代金券批次 ID
        String stockId = demo.createCouponStock();

        // 如果未创建成功，返回 null，请检查错误信息并重新配置代金券后发布
        if (stockId == null) {
            return;
        }

      	// 由于系统处理需要时间，建议间隔 1 分钟以上进行操作

        // 2. 代金券批次创建成功后，需要激活才能使用
        // 第二个参数为制券商户号，此处的示例为本商户制券，在实际使用中请注意制券商户号与批次号的对应关系
        demo.startStock(stockId, mchid);

      	// 由于系统处理需要时间，建议间隔 1 分钟以上进行操作

        // 3. 代金券激活后，商户可以发放代金券给用户
        // 第二个参数为制券商户号，此处的示例为本商户制券，在实际使用中请注意制券商户号与批次号的对应关系
        // 第三个参数是微信为发券方商户分配的公众账号ID
        // 第四个参数是需要发放代金券的用户在该 appid 下对应的 openid
        // 制券成功后返回发放成功的代金券 ID，失败返回 null
        String couponId = demo.sendCoupon(stockId, mchid, "example_appid", "example_user_openid");

      	// 由于系统处理需要时间，建议间隔 1 分钟以上进行操作

        // 4. 根据需要可以暂停运营中的代价券批次
        // 第二个参数为制券商户号，此处的示例为本商户制券，在实际使用中请注意制券商户号与批次号的对应关系
        demo.pauseStock(stockId, mchid);

      	// 由于系统处理需要时间，建议间隔 1 分钟以上进行操作

        // 5. 批次暂停后，可以根据需要重启代金券批次
      	// 重启代金券批次后，可以再次发放代金券给用户
        // 第二个参数为制券商户号，此处的示例为本商户制券，在实际使用中请注意制券商户号与批次号的对应关系
        demo.restartStock(stockId, mchid);

    }

    public String createCouponStock() {

        CreateCouponStock client = new CreateCouponStock(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        // 请参阅实际文档进行传参 https://pay.weixin.qq.com/doc/v3/merchant/4012534633
        CreateCouponStockRequest request = new CreateCouponStockRequest();

        request.stockName = "微信支付代金券";                                // 代金券批次名称
        request.comment = "零售批次2";                                      // 代金券批次注释
        request.belongMerchant = "merchant_id";                           // 代金券批次所属商户号
        request.availableBeginTime = "2025-08-14T17:05:00.120+08:00";     // 代金券批次生效时间
        request.availableEndTime = "2025-08-17T13:29:35.120+08:00";       // 代金券批次结束时间

        // 代金券批次的发放规则
        request.stockUseRule = new com.java.demo.CreateCouponStock.StockRule();
        request.stockUseRule.maxCoupons = 100L;
        request.stockUseRule.maxAmount = 50000L;
        request.stockUseRule.maxAmountByDay = 500L;
        request.stockUseRule.maxCouponsPerUser = 10L;
        request.stockUseRule.naturalPersonLimit = true;
        request.stockUseRule.preventApiAbuse = true;

        // 代金券详情页的相关信息
        request.patternInfo = new com.java.demo.CreateCouponStock.PatternInfo();
        request.patternInfo.description = "微信支付营销代金券";
        request.patternInfo.merchantLogo = "CDN地址";
        request.patternInfo.merchantName = "微信支付";
        request.patternInfo.backgroundColor = com.java.demo.CreateCouponStock.BackgroundColor.COLOR010;
        request.patternInfo.couponImage = "https://qpic.cn/xxx";
        request.patternInfo.jumpTarget = com.java.demo.CreateCouponStock.JumpTarget.PAYMENT_CODE;
        request.patternInfo.miniProgramAppid = "wx23232232323";
        request.patternInfo.miniProgramPath = "/path/index/index";

        // 代金券核销规则
        request.couponUseRule = new com.java.demo.CreateCouponStock.CouponRule();
        request.couponUseRule.couponAvailableTime = new com.java.demo.CreateCouponStock.FavorAvailableTime();
        request.couponUseRule.couponAvailableTime.fixAvailableTime = new com.java.demo.CreateCouponStock.FixedAvailableTime();
        request.couponUseRule.couponAvailableTime.fixAvailableTime.availableWeekDay = new ArrayList<>();
        {
            request.couponUseRule.couponAvailableTime.fixAvailableTime.availableWeekDay.add(1L);
        }
        ;
        request.couponUseRule.couponAvailableTime.fixAvailableTime.beginTime = 0L;
        request.couponUseRule.couponAvailableTime.fixAvailableTime.endTime = 3600L;
        request.couponUseRule.couponAvailableTime.secondDayAvailable = false;
        request.couponUseRule.couponAvailableTime.availableTimeAfterReceive = 7L;
        request.couponUseRule.fixedNormalCoupon = new com.java.demo.CreateCouponStock.FixedValueStockMsg();
        request.couponUseRule.fixedNormalCoupon.couponAmount = 500L;
        request.couponUseRule.fixedNormalCoupon.transactionMinimum = 1000L;
        request.couponUseRule.goodsTag = new ArrayList<>();
        {
            request.couponUseRule.goodsTag.add("123321");
        }
        ;
        request.couponUseRule.tradeType = new ArrayList<>();
        {
            request.couponUseRule.tradeType.add(com.java.demo.CreateCouponStock.TradeType.MICROAPP);
        }
        ;
        request.couponUseRule.combineUse = true;
        request.couponUseRule.availableItems = new ArrayList<>();
        {
            request.couponUseRule.availableItems.add("123321");
        }
        ;
        request.couponUseRule.unavailableItems = new ArrayList<>();
        {
            request.couponUseRule.unavailableItems.add("789987");
        }
        ;
        request.couponUseRule.availableMerchants = new ArrayList<>();
        {
            request.couponUseRule.availableMerchants.add("2600044199");
        }
        ;
        request.couponUseRule.limitCard = new com.java.demo.CreateCouponStock.CardLimitation();
        request.couponUseRule.limitCard.name = "精粹白金";
        request.couponUseRule.limitCard.bin = new ArrayList<>();
        {
            request.couponUseRule.limitCard.bin.add("62542688");
        }
        ;
        request.couponUseRule.limitPay = new ArrayList<>();
        {
            request.couponUseRule.limitPay.add("BCZ_DEBIT");
        }
        ;

        request.noCash = false;         // 营销经费，true：免充值代金券；false：预充值代金券。
        request.stockType = "NORMAL";   // 批次类型，目前仅支持：NORMAL（固定面额满减券批次）

        // 商户创建批次凭据号（格式：商户id+日期+流水号），可包含英文字母，数字，|，_，*，-等内容，不允许出现其他不合法符号，商户侧需保持
        // 商户单据号全局唯一。
        request.outRequestNo = "example_out_request_no2";

        // 扩展属性字段，按json格式。该字段暂未开放，不需要填写
        request.extInfo = "{'exinfo1':'1234','exinfo2':'3456'}";
        try {
            CreateCouponStockResponse response = client.run(request);

            // 请求成功，可以获取创建代金券批次的批次号和创建时间信息。
            String stockId = response.stockId;
            String createTime = response.createTime;

            return stockId;

        } catch (WXPayUtility.ApiException e) {
            // 创建代金券批次失败异常处理逻辑
            // 由于 api 文档可能更新，所有的错误可以参考 https://pay.weixin.qq.com/doc/v3/merchant/4012534633
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：请结合返回的错误信息，参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NO_AUTH")) {
                // 错误：你配置的信息需要开通特殊权限
                // 解决方式：参考 https://developers.weixin.qq.com/community/develop/article/doc/000c6aefe208488a14eb7819651013
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试（建议等待 1 分钟后原单重试）
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();

            // 创建失败
            return null;
        }
    }

    public void startStock(String stockId, String stockCreatorMchid) {

        StartStock client = new StartStock(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        StartStockRequest request = new StartStockRequest();
        request.stockId = stockId;                      // 需要激活的优惠券批次号
        request.stockCreatorMchid = stockCreatorMchid;  // 优惠券制券商户号
        try {
            StartStockResponse response = client.run(request);

            // 请求成功，系统会返回该批次的激活时间和批次 ID
            String startTimeResp = response.startTime;
            String stockIdResp = request.stockId;

        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合给定的提示信息，并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709 解决
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统异常，请稍后重试
                // 解决方式：请稍后原单重试
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的报错信息修改请求后重试
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次ID是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 未知错误码
                // 解决方式：请查看返回的错误信息
            }

            e.printStackTrace();

        }
    }

    public void pauseStock(String stockId, String stockCreatorMchid) {

        PauseStock client = new PauseStock(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        PauseStockRequest request = new PauseStockRequest();
        request.stockId = stockId;                      // 需要暂停的代金券批次 ID
        request.stockCreatorMchid = stockCreatorMchid;  // 批次的创建商户号
        try {
            PauseStockResponse response = client.run(request);

            // 请求成功，系统会返回暂停时间和批次号
            String pauseTimeResp = response.pauseTime;
            String stockIdResp = response.stockId;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
        }
    }

    public void restartStock(String stockId, String stockCreatorMchid) {

        RestartStock client = new RestartStock(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        RestartStockRequest request = new RestartStockRequest();
        request.stockId = stockId;                      // 需要重启的代金券批次ID
        request.stockCreatorMchid = stockCreatorMchid;  // 该代金券的制券商户号
        try {
            RestartStockResponse response = client.run(request);

            // 请求成功，系统返回重启时间和代金券批次ID
            String stockIdResp = request.stockId;
            String restartTimeResp = response.restartTime;

        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
        }
    }

    public String sendCoupon(String stockId, String stockCreatorMchid, String appid, String userOpenid) {

        SendCoupon client = new SendCoupon(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        SendCouponRequest request = new SendCouponRequest();

        // 请参阅实际文档进行传参 https://pay.weixin.qq.com/doc/v3/merchant/4012463767
        request.appid = appid;          // 微信为发券方商户分配的公众账号ID
        request.openid = userOpenid;    // 需要发放代金券的用户在该 appid 下对应的 openid

        // 优惠券信息
        request.stockId = stockId;                      // 券批次号
        request.stockCreatorMchid = stockCreatorMchid;  // 制券商户号
        request.outRequestNo = "out_request_no";        // 商户此次发放凭据号（格式：商户id+日期+流水号），商户侧需保持唯一性
        request.couponValue = 1L;                       // 指定面额发券场景，券面额，其他场景不需要填，单位：分。 （该字段暂未开放，注释掉不填写即可）
        request.couponMinimum = 1L;                     // 指定面额发券批次门槛，其他场景不需要，单位：分。 （该字段暂未开放，注释掉不填写即可）

        try {
            SendCouponResponse response = client.run(request);

            // 请求成功，系统会返回发放成功的代金券券号
            String couponId = response.couponId;

            return couponId;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode() == null) {
                // 错误码为空，无法判断具体错误
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
                // 可以参考 https://developers.weixin.qq.com/community/pay/doc/0000046f16862087608adcdf959c08 中的 Q4
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 可能的错误描述以及解决方式：
                // - 批次预算不足：批次预算已发放完，请补充批次预算
                // - 发券超过单天限额：已超过该批次设置的单天发放限制额度，无法发放
                // - 账户余额不足，请充值：商户号余额不足，无法继续发券，请充值
                // - 批次预算耗尽：该批次的预算已经耗尽，无法发放
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RULE_LIMIT")) {
                /*
                 * 错误码：RULE_LIMIT
                 * 可能的错误描述及解决方案：
                 * - 用户已达最大领券次数：该用户已达到该批次的领取上限，请参考常见问题Q6
                 * - 被自然人规则拦截：该自然人已达到该批次的领取上限，请参考常见问题Q6
                 */
            } else if (e.getErrorCode().equals("USER_ACCOUNT_ABNORMAL")) {
                /*
                 * 错误码：USER_ACCOUNT_ABNORMAL
                 * 可能的错误描述及解决方案：
                 * - 用户非法：用户命中微信支付风控模型，请参考常见问题Q5
                 * - 用户未实名：该用户无实名信息，无法领券。商家可联系微信支付或让用户联系微信支付客服处理。
                 */
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                /*
                 * 错误码：RESOURCE_NOT_EXISTS
                 * 错误描述：批次不存在
                 * 解决方案：请检查批次及制券商户号信息
                 */
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                /*
                 * 错误码：FREQUENCY_LIMITED
                 * 错误描述：当前请求人数过多，请稍后重试
                 * 解决方案：请降低API调用频率
                 */
            } else {
                // 未知错误码
                // 解决方案：请查看返回的错误信息
            }
            e.printStackTrace();
            return null;
        }
    }
}
```

# 4\. 处理代金券的核销通知信息

如果商户配置了代金券消息通知地址（见第二章），用户使用券后，微信会把相关核销信息发送给商户，商户需要接收处理，并按照文档规范返回应答。

收到通知回调后，需要依次经过验签，解密的步骤，并进行应答：

- 验签通过：商户需告知微信支付接收回调成功，HTTP应答状态码需返回200或204，无需返回应答报文。

- 验签不通过：商户需告知微信支付接收回调失败，HTTP应答状态码需返回5XX或4XX，同时需返回应答报文。


为了保证业务信息的安全性，微信支付将业务信息进行了AES-256-GCM加密，并通过参数resource将加密信息回调给商户，商户需要进行解密后才能获取到业务信息。

对于回调验签、应答以及解密的参数和机制，可以参考文档 [核销事件回调通知](https://pay.weixin.qq.com/doc/v3/merchant/4012285250.md)。

以下给出了处理代金券核销通知信息的示例代码。

```
package com.java.coupon;

import java.security.PublicKey;
import com.google.gson.annotations.SerializedName;

import com.java.utils.WXPayUtility;
import com.java.utils.WXPayUtility.Notification;

import java.util.List;
import okhttp3.Headers;

public class HandleCallback {

    private String wechatPayPublicKeyId;
    private PublicKey wechatPayPublicKey;
    private String apiv3AesKey;

    public HandleCallback() {
        this.wechatPayPublicKeyId = "填入 微信支付公钥ID";
        this.apiv3AesKey = "填入 APIv3密钥";

        String wechatPayPublicKeyPath = "填入 微信支付公钥文件路径";
        this.wechatPayPublicKey = WXPayUtility.loadPublicKeyFromPath(wechatPayPublicKeyPath);
    }

    public int callback(Headers headers, String reqBody) {
        try {
            // 解密
            Notification notification = WXPayUtility.parseNotification(apiv3AesKey, wechatPayPublicKeyId,
                    wechatPayPublicKey, headers, reqBody);
            String data = notification.getPlaintext();
            CallbackData callbackData = WXPayUtility.fromJson(data, CallbackData.class);

            // 获取其中的字段并处理
            // 具体字段以及含义请参考 https://pay.weixin.qq.com/doc/v3/merchant/4012285250
            String stockCreatorMchid = callbackData.stockCreatorMchid;
            String stockId = callbackData.stockId;
            String couponId = callbackData.couponId;
            // ......

            // 成功，响应：200，无响应体
            return 200;
        } catch (Exception e) {
            e.printStackTrace();
            // 失败，响应：5xx或4xx，响应：
            // {
            // "code": "FAIL",
            // "message": "失败"
            // }
            return 500;
        }
    }

    public static class CallbackData {

        @SerializedName("stock_creator_mchid")
        public String stockCreatorMchid;

        @SerializedName("stock_id")
        public String stockId;

        @SerializedName("coupon_id")
        public String couponId;

        @SerializedName("singleitem_discount_off")
        public SingleItemDiscountOff singleItemDiscountOff;

        @SerializedName("discount_to")
        public DiscountTo discountTo;

        @SerializedName("coupon_name")
        public String couponName;

        @SerializedName("status")
        public String status;

        @SerializedName("description")
        public String description;

        @SerializedName("create_time")
        public String createTime;

        @SerializedName("coupon_type")
        public String couponType;

        @SerializedName("no_cash")
        public Boolean noCash;

        @SerializedName("available_begin_time")
        public String availableBeginTime;

        @SerializedName("available_end_time")
        public String availableEndTime;

        @SerializedName("singleitem")
        public Boolean singleItem;

        @SerializedName("normal_coupon_information")
        public Long normalCouponInformation;

        @SerializedName("goods_detail")
        public List<GoodsDetail> goodsDetail;

        @SerializedName("business_type")
        public String businessType;
    }

    public static class ConsumeInformation {

        @SerializedName("consume_time")
        public String consumeTime;

        @SerializedName("consume_mchid")
        public String consumeMchid;

        @SerializedName("transaction_id")
        public String transactionId;
    }

    public static class NormalCouponInformation {

        @SerializedName("coupon_amount")
        public Long couponAmount;

        @SerializedName("transaction_minimum")
        public Long transactionMinimum;
    }

    public static class SingleItemDiscountOff {

        @SerializedName("single_price_max")
        public Long singlePriceMax;
    }

    public static class DiscountTo {

        @SerializedName("cut_to_price")
        public Long cutToPrice;

        @SerializedName("max_price")
        public Long maxPrice;
    }

    public static class GoodsDetail {

        @SerializedName("goods_id")
        public String goodsId;

        @SerializedName("quantity")
        public Integer quantity;

        @SerializedName("price")
        public Integer price;

        @SerializedName("discount_amount")
        public Integer discount_amount;

    }
}
```

# 5\. 查询代金券以及批次的具体信息

对于代金券批次，商家可以查询指定批次的信息，包括：

- 指定查询条件，查询满足条件的批次信息；

- 查询单个指定批次号的批次信息；

- 查询指定批次的可用商户信息；

- 查询指定批次的可用单品信息。


对于代金券，商家可以查询指定代金券的信息，包括：

- 指定查询条件，查询指定用户符合条件的代金券；

- 查询单个指定代金券的信息。


以下提供了这些查询操作的相关代码：

```
package com.java.coupon;

import com.java.utils.WXPayUtility; // 引用微信支付工具库，参考：https://pay.weixin.qq.com/doc/v3/merchant/4014931831

// 使用商户平台条件查询批次列表接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012460489
import com.java.demo.ListStocks;
import static com.java.demo.ListStocks.*;

// 使用商户平台查询批次详细接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012460564
import com.java.demo.QueryStock;
import static com.java.demo.QueryStock.*;

// 使用商户平台查询代金券可用商户接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012463358
import com.java.demo.ListAvailableMerchants;
import static com.java.demo.ListAvailableMerchants.*;

// 使用商户平台查询代金券可用单品接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012463442
import com.java.demo.ListAvailableSingleitems;
import static com.java.demo.ListAvailableSingleitems.*;

// 使用商户平台 根据商户号查用户的券 接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012534690
import com.java.demo.ListCouponsByFilter;
import static com.java.demo.ListCouponsByFilter.*;

// 使用商户平台查询代金券详情接口。参考：https://pay.weixin.qq.com/doc/v3/merchant/4012486942
import com.java.demo.QueryCoupon;
import static com.java.demo.QueryCoupon.*;

public class QueryCouponAndStockDemo {

    // TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考
    // https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    private static String mchid = "19xxxxxxxx";
    // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013053053
    private static String certificateSerialNo = "1DDE55AD98Exxxxxxxxxx";
    // 商户API证书私钥文件路径，本地文件路径
    private static String privateKeyFilePath = "/path/to/apiclient_key.pem";
    // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013038816
    private static String wechatPayPublicKeyId = "PUB_KEY_ID_xxxxxxxxxxxxx";
    // 微信支付公钥文件路径，本地文件路径
    private static String wechatPayPublicKeyFilePath = "/path/to/wxp_pub.pem";

    public static void main(String[] args) {

        QueryCouponAndStockDemo demo = new QueryCouponAndStockDemo();

        String stockId = "example_stockId";     // 需要查询的批次号
        String creatorMchid = mchid;            // 创建批次的商户号
        String couponId = "example_couponId";   // 需要查询的优惠券券号
        String appid = "example_appid";         // 微信为发券方商户分配的公众账号ID
        String openid = "example_openid";       // 用户在该 appid 下对应的 openid，用于标识需要查询的用户

        // 1. 查询代金券批次的相关操作
        // 1.1 条件查询代金券批次列表
        // 可以根据批次创建商户号、创建的时间区间和当前状态查询商户批次列表
        ListStocks.StockCollection stockCollection = demo.listStocks();

        // 1.2 查询批次详情
        QueryStock.Stock stock = demo.queryStock(creatorMchid, stockId);

        // 1.3 查询代金券批次可用商户
        // 查询特定批次的可用商户集合
        AvailableMerchantCollection merchantCollection = demo.listAvailableMerchants(creatorMchid, stockId);

        // 1.4 查询代金券批次可用单品
        // 查询特定批次的可用单品集合
        AvailableSingleitemCollection singleitemCollection = demo.listAvailableSingleItems(creatorMchid, stockId);

        // 2. 查询代金券的相关操作
        // 2.1 根据商户号查用户的券
        // 可以根据商户号、批次号、券状态等条件查询指定用户符合条件的代金券
        ListCouponsByFilter.CouponCollection couponCollection = demo.listCouponByFilter();

        // 2.2 查询代金券详情
        // 查询指定用户指定券号的代金券详情
        QueryCoupon.Coupon coupon = demo.queryCoupon(couponId, appid, openid);

    }

    // 查询满足条件的批次列表
    // 返回所有满足条件的批次集合
    public ListStocks.StockCollection listStocks() {

        ListStocks client = new ListStocks(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        ListStocksRequest request = new ListStocksRequest();

        // 请参阅实际文档进行传参 https://pay.weixin.qq.com/doc/v3/merchant/4012460489
        // 分页信息
        request.offset = 0L; // 分页页码，页码从0开始
        request.limit = 8L; // 分页大小，目前最大为 10

        // 批次信息
        // 时间格式遵循rfc3339标准格式，格式为yyyy-MM-DDTHH:mm:ss+TIMEZONE
        request.stockCreatorMchid = "merchant_id";                  // 批次创建商户号
        request.createStartTime = "2025-05-20T13:29:35.120+08:00";  // 查询的起始创建时间
        request.createEndTime = "2025-08-25T13:29:35.120+08:00";    // 查询的终止创建时间

        // 批次状态，具体状态描述请参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012460489
        request.status = "running";

        try {
            StockCollection response = client.run(request);
            // 请求成功，返回 StockCollection 类的实例 response 储存查到的所有优惠券的集合，
            // 这个类的定义详见 https://pay.weixin.qq.com/doc/v3/merchant/4012460489 的代码描述

            return response;

        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();

            return null;
        }
    }

    // 查询商户 creatorMchid 创建的批次号为 stockId 的批次信息
    // 返回该批次的详细信息
    public QueryStock.Stock queryStock(String creatorMchid, String stockId) {

        QueryStock client = new QueryStock(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        QueryStockRequest request = new QueryStockRequest();
        request.stockId = stockId;                  // 需要查询的代金券批次ID
        request.stockCreatorMchid = creatorMchid;   // 该代金券的制券商户号
        try {
            QueryStock.Stock response = client.run(request);
            // 请求成功，返回 Stock 类型的变量 response 表示查询到的批次信息
            // Stock 类型的定义详见 https://pay.weixin.qq.com/doc/v3/merchant/4012460564 的代码描述

            return response;

        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
            return null;
        }
    }

    // 查询商户 creatorMchid 创建的批次号为 stockId 批次的可用商户信息
    // 可以设置分页参数
    // 返回该批次可用的商户集合
    public AvailableMerchantCollection listAvailableMerchants(String creatorMchid, String stockId) {

        ListAvailableMerchants client = new ListAvailableMerchants(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        ListAvailableMerchantsRequest request = new ListAvailableMerchantsRequest();

        // 请参阅实际文档进行传参 https://pay.weixin.qq.com/doc/v3/merchant/4012463358
        // 优惠券批次信息
        request.stockId = stockId;                  // 需要查询的代金券批次ID
        request.stockCreatorMchid = creatorMchid;   // 该代金券的制券商户号

        // 分页信息
        request.offset = 0L; // 分页页码，从 0 开始，目前最大1000
        request.limit = 10L; // 分页大小，目前最大为 50

        try {
            AvailableMerchantCollection response = client.run(request);

            // 请求成功，返回该批次可用的商户号集合
            for (String availableMerchant : response.data) {
                System.out.println(availableMerchant);
                // 对可用商户进行其他逻辑操作
            }

            return response;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();

            return null;
        }
    }

    // 查询商户 creatorMchid 创建的批次号为 stockId 批次的可用单品信息
    // 可以设置分页参数
    // 返回该批次可用的单品集合
    public AvailableSingleitemCollection listAvailableSingleItems(String creatorMchid, String stockId) {

        ListAvailableSingleitems client = new ListAvailableSingleitems(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        ListAvailableSingleitemsRequest request = new ListAvailableSingleitemsRequest();

        // 请参阅实际文档进行传参 https://pay.weixin.qq.com/doc/v3/merchant/4012463442
        // 优惠券批次信息
        request.stockId = stockId;                  // 需要查询的代金券批次ID
        request.stockCreatorMchid = creatorMchid;   // 该代金券的制券商户号

        // 分页信息
        request.offset = 0L; // 分页页码，从 0 开始，目前最大为 500
        request.limit = 10L; // 分页大小，目前最大为 100

        try {
            AvailableSingleitemCollection response = client.run(request);

            // 请求成功，返回代金券能够使用的商品集合
            System.out.println(response.toString());
            for (String singleItem : response.data) {
                // 对每个物品进行后续逻辑操作
            }

            return response;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
                // 在这个接口中，如果优惠券没有指定单品信息，也会报参数错误
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试（建议等待一分钟后原单重试）
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
            return null;
        }
    }

    // 根据商户号查用户的券，查询用户满足条件的券列表
    // 返回所有满足条件的券集合
    public ListCouponsByFilter.CouponCollection listCouponByFilter() {

        ListCouponsByFilter client = new ListCouponsByFilter(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        ListCouponsByFilterRequest request = new ListCouponsByFilterRequest();

        // 请参阅实际文档进行传参 https://pay.weixin.qq.com/doc/v3/merchant/4012534690
        request.appid = "appid_example";      // 微信为发券方商户分配的公众账号ID
        request.openid = "openid_example";    // 用户在该 appid 下对应的 openid

        // 优惠券信息
        request.stockId = "stock_id"; // 优惠券批次号

        // 优惠券状态，选填creator_mchid时, SENDED：返回可用；USED：返回可用+已实扣。
        // 选填available_mchid时，该字段不生效，仅返回 可用 状态的券或消费金。
        request.status = "USED";

        // 是否查询消费金
        request.businessType = ListCouponsByFilter.BusinessType.MULTIUSE;     // 选填，如果填写为 BusinessType.MULTIUSE，则表示查询用户的消费金

        // 批次对应的商户
        // creator_mchid 与 available_mchid 二选一，如果同时存在，优先使用 creator_mchid。
        request.creatorMchid = "merchant_id_1";        // 批次创建方商户号
        request.availableMchid = "merchant_id_2";      // 可用商户号

        // 分页信息，填写 available_mchid，则分页信息不生效
        request.offset = 0L;      // 页码，默认 0
        request.limit = 20L;      // 分页大小，默认 20

        try {
            CouponCollection response = client.run(request);

            // 请求成功，返回查询到的优惠券集合
            for (ListCouponsByFilter.Coupon coupon : response.data) {
                // 对每一张券继续进行后续操作
            }

            return response;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("USER_ACCOUNT_ABNORMAL")) {
                // 错误：用户非法
                // 解决方式：该用户账号异常。商家可联系微信支付或让用户联系微信支付客服处理。
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试（建议等待一分钟后原单重试）
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
            return null;
        }
    }

    // 查询指定用户指定券号的代金券详情
    // 返回该代金券的详细信息
    public QueryCoupon.Coupon queryCoupon(String couponId, String appid, String openid) {

        QueryCoupon client = new QueryCoupon(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        QueryCouponRequest request = new QueryCouponRequest();
        request.couponId = couponId;   	// 需要查询的优惠券 ID
        request.openid = openid;  		// 用户在该 appid 下对应的 openid
        request.appid = appid;  		// 微信为发券方商户分配的公众账号ID
        try {
            QueryCoupon.Coupon response = client.run(request);
            // 请求成功，系统返回 Coupon 类型的实例表示查询到的代金券。
            // 该类型的定义详见 https://pay.weixin.qq.com/doc/v3/merchant/4012486942

            return response;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("USER_ACCOUNT_ABNORMAL")) {
                // 错误：用户非法
                // 解决方式：该用户账号异常。商家可联系微信支付或让用户联系微信支付客服处理。
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED") || e.getErrorCode()
                    .equals("FREQUENCY_LIMIT_EXCEED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试（建议等待一分钟后原单重试）
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
            return null;
        }
    }
}
```

需要注意的是，如有选填参数未填写导致运行时报错，可以尝试将 `WXPayUtility` 中的函数 `urlEncode` 替换为如下版本以解决问题：

```
public static String urlEncode(Map<String, Object> params) {
    if (params == null || params.isEmpty()) {
        return "";
    }

    int index = 0;
    StringBuilder result = new StringBuilder();
    for (Map.Entry<String, Object> entry : params.entrySet()) {
        if (entry.getValue() != null) {
            result.append(entry.getKey())
                    .append("=")
                    .append(urlEncode(entry.getValue().toString()));
        }
        index++;
        if (index < params.size()) {
            result.append("&");
        }
    }
    return result.toString();
}
```

# 6\. 下载批次核销和退款明细

活动结束后，商户可以获取某批次的退款和核销明细数据，包括订单号、单品信息、银行流水号等，用于对账/数据分析。

需要注意的是：

1. 账单文件的下载地址的有效时间为30s。

2. 强烈建议商户将实际账单文件的哈希值和之前从接口获取到的哈希值进行比对，以确认数据的完整性。

3. 该接口响应的信息请求头中不包含微信接口响应的签名值，因此需要跳过验签的流程。

4. 该接口需要活动结束后次日10点才可以下载。

5. 该接口只能使用创建活动方进行调用。


以下提供了请求下载地址的示例代码。

```
package com.java.coupon;

import com.java.utils.WXPayUtility; // 引用微信支付工具库，参考：https://pay.weixin.qq.com/doc/v3/merchant/4014931831

// 使用商户平台下载批次退款明细接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012463523
import com.java.demo.RefundFlow;
import static com.java.demo.RefundFlow.*;

// 使用商户平台下载批次核销明细接口，参考：https://pay.weixin.qq.com/doc/v3/merchant/4012463585
import com.java.demo.UseFlow;
import static com.java.demo.UseFlow.*;

public class DownloadFlowDemo {

    // TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考
    // https://pay.weixin.qq.com/doc/v3/merchant/4013070756
    private static String mchid = "19xxxxxxxx";
    // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013053053
    private static String certificateSerialNo = "1DDE55AD98Exxxxxxxxxx";
    // 商户API证书私钥文件路径，本地文件路径
    private static String privateKeyFilePath = "/path/to/apiclient_key.pem";
    // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013038816
    private static String wechatPayPublicKeyId = "PUB_KEY_ID_xxxxxxxxxxxxx";
    // 微信支付公钥文件路径，本地文件路径
    private static String wechatPayPublicKeyFilePath = "/path/to/wxp_pub.pem";

    public static void main(String[] args) {

        DownloadFlowDemo demo = new DownloadFlowDemo();

        String stockId = "example_stock_id";    // 需要查询明细的批次号

        // 在批次结束后次日10点后，商户可以下载批次的退款明细和核销明细
        // 账单文件的下载地址的有效时间为30s，请商户及时下载
        // 强烈建议商户将实际账单文件的哈希值和之前从接口获取到的哈希值进行比对，以确认数据的完整性。

        // 1. 查询批次退款明细的下载地址
        // 返回下载地址、哈希类型以及哈希值。
        RefundFlowResponse refundFlowData = demo.refundFlow(stockId);
        String refundFlowUrl = refundFlowData.url;
        String refundFlowHashType = refundFlowData.hashType;
        String refundFlowHashValue = refundFlowData.hashValue;

        // 2. 查询批次核销明细的下载地址
        // 返回下载地址、哈希类型以及哈希值。
        UseFlowResponse useFlowData = demo.useFlow(stockId);
        String useFlowUrl = useFlowData.url;
        String useFlowHashType = useFlowData.hashType;
        String useFlowHashValue = useFlowData.hashValue;

    }

    // 查询批次退款明细的下载地址
    // 返回下载地址
    public RefundFlow.RefundFlowResponse refundFlow(String stockId) {

        RefundFlow client = new RefundFlow(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        RefundFlowRequest request = new RefundFlowRequest();

        // 需要查询退款明细的优惠券批次号
        request.stockId = stockId;

        try {
            RefundFlowResponse response = client.run(request);
            return response;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
            return null;
        }
    }

    public UseFlow.UseFlowResponse useFlow(String stockId) {
        // 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/merchant/4013070756
        UseFlow client = new UseFlow(
                mchid,
                certificateSerialNo,
                privateKeyFilePath,
                wechatPayPublicKeyId,
                wechatPayPublicKeyFilePath
        );

        UseFlowRequest request = new UseFlowRequest();

        // 需要查询的优惠券批次号
        request.stockId = stockId;

        try {
            UseFlowResponse response = client.run(request);
            return response;
        } catch (WXPayUtility.ApiException e) {
            // 请求失败，根据状态码执行不同的逻辑
            if (e.getErrorCode().equals("SYSTEM_ERROR")) {
                // 错误：系统错误
                // 解决方式：稍后（建议等 1 分钟后）原单重试
            } else if (e.getErrorCode().equals("PARAM_ERROR")) {
                // 错误：参数错误
                // 解决方式：请根据错误提示正确传入参数
            } else if (e.getErrorCode().equals("INVALID_REQUEST")) {
                // 错误：HTTP 请求不符合微信支付 APIv3 接口规则
                // 解决方式：结合错误信息并参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081709，修改参数达到符合业务规则之后再重试
            } else if (e.getErrorCode().equals("SIGN_ERROR")) {
                // 错误：验证不通过
                // 解决方式：检查平台商户证书序列号，证书私钥文件，公钥ID，公钥文件，同时确认下签名过程是不是按照微信支付的签名方式
                // 可以参阅 https://pay.weixin.qq.com/doc/v3/merchant/4012081606 下的 “如何签名” 和 “如何验签” 部分
            } else if (e.getErrorCode().equals("APPID_MCHID_NOT_MATCH")) {
                // 错误：商户号与AppID不匹配
                // 解决方式：请绑定调用接口的商户号和AppID后重试
            } else if (e.getErrorCode().equals("MCH_NOT_EXISTS")) {
                // 错误：商户号不合法
                // 解决方式：请输入正确的商户号
            } else if (e.getErrorCode().equals("NOT_ENOUGH")) {
                // 错误：批次预算不足
                // 解决方式：请补充预算
            } else if (e.getErrorCode().equals("REQUEST_BLOCKED")) {
                // 错误：请求失败
                // 解决方式：请根据具体的错误信息修改请求
            } else if (e.getErrorCode().equals("RESOURCE_NOT_EXISTS")) {
                // 错误：批次不存在
                // 解决方式：请检查批次 ID 是否正确
            } else if (e.getErrorCode().equals("FREQUENCY_LIMITED")) {
                // 错误：请求过于频繁
                // 解决方式：稍后重试
            } else {
                // 其他错误码
                // 解决方式：请查看返回的错误信息
            }
            e.printStackTrace();
            return null;
        }
    }
}

```